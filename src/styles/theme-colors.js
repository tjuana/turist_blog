import * as MORNING_THEME from './morning.theme.style'
import * as AFTERNOON_THEME from './afternoon.theme.style'
import * as EVENING_THEME from './evening.theme.style'
import * as NIGHT_THEME from './night.theme.style'

const THEME = {
    MORNING: MORNING_THEME,
    AFTERNOON: AFTERNOON_THEME,
    EVENING: EVENING_THEME,
    NIGHT: NIGHT_THEME
}

export default THEME